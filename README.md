# template demo
function a() {
    return {
        parameters:{
            className: String,
            labels: Array,
            fields: Array,
            fieldValids: Array,
        },
        requestLib:{

        },
        compile(params) {

        }
    }
}


# const { clipboard } = window.require('electron');

# fiddler 可以做接口数据 多倍 返回。
