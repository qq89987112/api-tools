import axios from "axios"
export default {
<% apis.forEach((item)=>{%>
  <%= item.comment %>
  <%= item.name %>(<%= item.params&&item.params.concat(item.params2||[]).join(',') %>){
    return axios.<%= item.method.toLowerCase() %>(`<%= item.url %><%= getParams(item) %>`<% if(item.params&&item.method.toLowerCase()==='post'){%>
    ,{
    <%= item.params.join(',') %>
  }
  <%}%>)
  },
<%}) %>
}
