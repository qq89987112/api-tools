import Vue from 'vue'
import Router from 'vue-router'
<%= Object.entries(pagesOut).map((item)=>{
    return `import ${item[0]} from "./${item[0]}.vue"`
}).join("\r\n") %>
Vue.use(Router)

export default new Router({
  routes: [
  <% Object.entries(pagesOut).forEach((item)=>{%>
        {
              path: '/<%= item[0] %>',
              name: '<%= item[0] %>',
              component: <%= item[0] %>
        },
   <%})%>
  ]
})
